// src/api/courseApi.js
// Replaced with mock service for demo mode.
// To re-enable real APIs: replace mockCourseApi with axiosInstance calls.
import { mockCourseApi } from "../mock/mockService";

export const courseApi = mockCourseApi;
