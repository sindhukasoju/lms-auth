// src/api/authApi.js
// Replaced with mock service for demo mode.
// To re-enable real APIs: replace mockAuthService with axiosInstance calls.
import { mockAuthService } from "../mock/mockService";

export const authApi = mockAuthService;
